# local development config file for the SSE client
other_proxy_url: "http://localhost:8080"
broker:
  username: intersect_username
  password: intersect_password
  host: "127.0.0.1"
  # note: differs from other config file (two separate brokers used)
  port: 5673
log_level: "debug"
production: false
